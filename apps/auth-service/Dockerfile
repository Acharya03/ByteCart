# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @byte-cart/auth-service`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t byte-cart-auth-service`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system byte-cart-auth-service && \
          adduser --system -G byte-cart-auth-service byte-cart-auth-service

COPY apps/auth-service/dist byte-cart-auth-service/
COPY apps/auth-service/package.json byte-cart-auth-service/
RUN chown -R byte-cart-auth-service:byte-cart-auth-service .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix byte-cart-auth-service --omit=dev -f install

CMD [ "node", "byte-cart-auth-service" ]
